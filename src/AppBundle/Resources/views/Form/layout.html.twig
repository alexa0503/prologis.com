{% use "bootstrap_3_horizontal_layout.html.twig" %}
{% block form_widget_simple -%}
    {% if type is defined and 'file' == type %}
    	<div class="img-preview">{% if attr.value is defined and attr.value is not empty %}<img src="/uploads/{{ attr.value }}" style="max-width:300px;" />{% endif %}</div>
    {% endif %}
    	{{- parent() -}}
{%- endblock form_widget_simple %}